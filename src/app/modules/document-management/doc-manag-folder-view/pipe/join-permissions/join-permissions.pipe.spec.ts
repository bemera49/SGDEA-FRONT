import { JoinPermissionsPipe } from './join-permissions.pipe';

describe('JoinPermissionsPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinPermissionsPipe();
    expect(pipe).toBeTruthy();
  });
});
